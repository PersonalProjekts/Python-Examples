# --------------------------------------------------------------------------------
# Documentação ......: https://pypi.org/project/pytube/
# Atualizando o PIP .: python.exe -m pip install --upgrade pip
# Instalando PYTUBE .: python.exe -m pip install pytube
#
# Ajuste no arquivo cipher.py (linha 30)
#                  var_regex = re.compile("^\$*\w+\W")
# --------------------------------------------------------------------------------
